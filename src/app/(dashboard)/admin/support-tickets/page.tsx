"use client";

import React from "react";
import { SupportTickets } from "@/features";

// Reuse the row type from the shared SupportTicketTable component

export default function SupportTicketsPage() {
  return <SupportTickets />;
}
