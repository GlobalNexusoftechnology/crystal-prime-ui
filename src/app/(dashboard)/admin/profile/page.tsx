import { Profile } from "@/features";

/**
 * ProfilePage component
 *
 * This is a Next.js page that renders the user profile by importing
 * and displaying the <Profile /> component from the features module.
 *
 * Route: /profile
 *
 * Usage:
 * Simply navigate to /profile in the app to view the user's profile details.
 */
export default function ProfilePage() {
  return <Profile />;
}
